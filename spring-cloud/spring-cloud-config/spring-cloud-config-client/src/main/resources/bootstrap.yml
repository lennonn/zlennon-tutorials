server:
  port: 7004
spring:
  config:
    import: optional:configserver:http://localhost:7003
  cloud:
    config:
      label: master
      name: application-dev #需要从github上读取的资源名称，注意没有yml后缀名
      profile: dev   #本次访问的配置项
      uri: http://localhost:7003
